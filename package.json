{
  "name": "codeinquero-client",
  "version": "1.0.0",
  "description": "CodeInQuero",
  "author": "WinDevs",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server/index.js --watch server",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "heroku-postbuild": "npm run build"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.3.6",
    "cross-env": "^5.2.0",
    "express": "^4.16.4",
    "nuxt": "^2.4.0",
    "prettier": "^1.17.1"
  },
  "devDependencies": {
    "nodemon": "^1.18.9"
  }
}
